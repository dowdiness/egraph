import {
  "moonbitlang/core/builtin",
  "moonbitlang/core/hashset",
  "moonbitlang/core/strconv",
  "dowdiness/lambda/ast",
}

import {
  "moonbitlang/core/bench",
} for "test"

// 29 (unused_package): lambda/ast is used only in *_wbtest.mbt files

warnings = "-29"
